### Auto start applications
#
# Applets:
#
# NetworkManager applet
  exec --no-startup-id nm-applet --indicator

# Bluetooth applet
  exec --no-startup-id blueman-applet

# Display keys
  # exec --no-startup-id wshowkeys -b '#1e1e2e' -f '#cdd6f4' -s '#89b4fa' -F 'FiraCode Nerd Font 20' -t 1 -a bottom
#
# Apps:
#
  focus output $primary
  assign [app_id="Slack"] workspace number 7
  exec slack --ozone-platform=wayland --enable-features=UseOzonePlatform,WebRTCPipeWireCapturer
  assign [class="discord"] workspace number 8
  exec discord 
  assign [class="thunderbird"] workspace number 9
  exec thunderbird
  for_window [class="Spotify"] move window to workspace number 10
  exec spotify
  assign [class="firefox"] workspace number 1
  # exec firefox
